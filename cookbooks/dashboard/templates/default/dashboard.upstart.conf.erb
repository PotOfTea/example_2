#!upstart
description "Dashboard Server"

env USER=<%= node[:dashboard][:user] %>
start on filesystem or runlevel [2345]
stop on shutdown


script

	cd <%= node[:dashboard][:dir] %>
	exec sudo -u $USER sh -c "dashing start -p <%= node[:dashboard][:port] %>"

end script

